{% macro loadAssets() %}
    <script src="https://cdn.jsdelivr.net/npm/vanillatoasts@1.3.0/vanillatoasts.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vanillatoasts@1.3.0/vanillatoasts.css" />
    <script src="/assets/spinner.js{{ app.config.dialogbox_force_reload_js ? ('?=' ~ date().timestamp) : '' }}"></script>
    <link rel="stylesheet" href="/assets/spinner.css{{ app.config.dialogbox_force_reload_css ? ('?=' ~ date().timestamp) : '' }}" />
    <script src="/assets/dialogbox.js{{ app.config.dialogbox_force_reload_js ? ('?=' ~ date().timestamp) : '' }}"></script>
    <link rel="stylesheet" href="/assets/dialogbox.css{{ app.config.dialogbox_force_reload_css ? ('?=' ~ date().timestamp) : '' }}" />
{% endmacro %}

{% macro loadConfig() %}
    <script>
        var dialogboxConfig = {
            url: '{{ app.here }}'
        };
    </script>
{% endmacro %}